const greeting = require('../src/greeting');

describe('#greeting', () => {
  // descomente os testes e implemente-os

  // it('greet with "Tryber" as parameter')

  // it('greet with "Software Developer" as parameter')

  // it('greet with no parameter')
});
