/*
  A função duplicate recebe uma lista de parâmetros (tamanho variável)
  e retorna `true` se existe algum elemento duplicate e `false` caso contrário
 */

 function duplicate(params) {

 }

 module.exports = duplicate
